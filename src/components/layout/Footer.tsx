/**
 * Footer Component
 *
 * Main footer component for KITS Alumni website
 * Uses the modern StickyFooter component with:
 * - Sticky scroll effect
 * - Brand colors (#3F426B primary, #D4A72E secondary)
 * - Newsletter subscription
 * - Social media links
 * - Contact information
 * - "Crafted by Tabrez Khan" signature
 */

import { StickyFooter } from "@/components/ui/sticky-footer";

const Footer = () => {
    return <StickyFooter />;
};

export default Footer;
